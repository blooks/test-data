machine:
  node:
    version: 6
  version: trusty-645-0f3f4aa
  services:
    - mongo

deployment:
  versionbuild:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - mv file.npmrc .npmrc
      - npm publish --access=public